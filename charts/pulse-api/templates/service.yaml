apiVersion: v1
kind: Service
metadata:
  name: {{ include "pulse-api.fullname" . }}
  labels:
    {{- include "pulse-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "pulse-api.selectorLabels" . | nindent 4 }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
